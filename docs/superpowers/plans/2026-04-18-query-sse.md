# 쿼리 진행 상태 SSE 전환 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 폴링 기반 진행 상태 전송을 SSE(Server-Sent Events)로 전환하여 쿼리 1건당 HTTP 연결 30회를 1회로 축소.

**Architecture:** query-service에 EventEmitter 기반 pub/sub을 추가하고 Hono `stream()` 핸들러로 단방향 SSE 송출. ui-service는 Next.js streaming Response + TransformStream 버퍼 파서로 SSE를 프록시하면서 완료 시 assistant 메시지를 저장. 기존 `GET /query/status/:jobId`는 완전 제거.

**Tech Stack:** Hono `stream`, Node EventEmitter, Web Streams API (TransformStream), Browser EventSource, Node 20+ native fetch+ReadableStream (eval runner).

**스펙 참조:** `docs/superpowers/specs/2026-04-17-query-sse.md`

---

## File Structure

| 파일 | 역할 |
|---|---|
| `query-service/src/jobs/query-jobs.ts` | EventEmitter pub/sub 추가 (subscribe/update emit), TTL cleanup 시 listener 제거 |
| `query-service/src/sse/format.ts` | `formatEvent(job)` helper 신규 — `data: {...}\n\n` 직렬화 |
| `query-service/src/index.ts` | `GET /query/stream/:jobId` 추가 (Hono stream), `GET /query/status/:jobId` 제거 |
| `query-service/src/eval/runner.ts` | `callQueryService` 폴링 → 수동 SSE 파서로 변경 |
| `ui-service/app/api/query/stream/[jobId]/route.ts` | 신규 — Next.js streaming Response, TransformStream으로 chunk 버퍼 파싱 + assistant 저장 |
| `ui-service/app/api/query/status/[jobId]/route.ts` | 삭제 |
| `ui-service/app/components/ChatPanel.tsx` | 폴링 useEffect → EventSource (cleanCloseRef + 60s timeout) |
| `docs/STATUS.md` | 변경 이력 + Phase 4 항목 갱신 |
| `docs/ARCHITECTURE.md` | "쿼리 진행 상태 전송 — SSE" 결정 추가 |
| `docs/ROADMAP.md` | "쿼리 진행 상태 UX" 항목 SSE 전환 메모 |

---

## Task 1: JobRegistry EventEmitter pub/sub 추가

**Files:**
- Modify: `query-service/src/jobs/query-jobs.ts`

- [ ] **Step 1: EventEmitter import + 멤버 추가**

`query-service/src/jobs/query-jobs.ts` 상단 import 추가:

```typescript
import { EventEmitter } from "node:events";
```

`JobRegistry` 클래스 내부 `private jobs` 아래에 추가:

```typescript
  private emitter = new EventEmitter();
```

- [ ] **Step 2: update() 메서드에서 이벤트 emit**

기존 `update` 교체:

```typescript
  update(jobId: string, patch: Partial<QueryJob>): void {
    const existing = this.jobs.get(jobId);
    if (!existing) return;
    const updated = { ...existing, ...patch };
    this.jobs.set(jobId, updated);
    this.emitter.emit(jobId, updated);
  }
```

- [ ] **Step 3: subscribe() 메서드 추가**

`update` 메서드 아래에 추가:

```typescript
  subscribe(jobId: string, listener: (job: QueryJob) => void): () => void {
    this.emitter.on(jobId, listener);
    return () => this.emitter.off(jobId, listener);
  }
```

- [ ] **Step 4: TTL cleanup에서 listener 제거**

기존 `startCleanup` 내부 for 루프를 다음으로 교체:

```typescript
      for (const [id, job] of this.jobs) {
        if (job.completedAt && now - job.completedAt > TTL_MS) {
          this.jobs.delete(id);
          this.emitter.removeAllListeners(id);
        }
      }
```

- [ ] **Step 5: TypeScript 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/query-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 6: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add query-service/src/jobs/query-jobs.ts && git commit -m "feat(query): JobRegistry EventEmitter pub/sub for SSE"
```

---

## Task 2: SSE 이벤트 포맷 helper 추가

**Files:**
- Create: `query-service/src/sse/format.ts`

- [ ] **Step 1: format.ts 신규 작성**

```typescript
import type { QueryJob } from "../jobs/query-jobs.js";

/**
 * QueryJob을 SSE 이벤트 페이로드로 직렬화.
 * 형식: `data: {json}\n\n` (SSE 표준 message 이벤트 경계)
 */
export function formatEvent(job: QueryJob): string {
  const base: Record<string, unknown> = {
    status: job.status,
    currentStep: job.currentStep,
    stepLabel: job.stepLabel,
    progressIndex: job.progressIndex,
    totalSteps: job.totalSteps,
    questionType: job.questionType,
    retryCount: job.retryCount,
  };
  if (job.status === "completed" && job.result) base.result = job.result;
  if (job.status === "failed") base.error = job.error;
  return `data: ${JSON.stringify(base)}\n\n`;
}
```

- [ ] **Step 2: 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/query-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add query-service/src/sse/format.ts && git commit -m "feat(query): formatEvent helper for SSE payloads"
```

---

## Task 3: SSE stream 엔드포인트 추가 + status 엔드포인트 제거

**Files:**
- Modify: `query-service/src/index.ts:178-201` (status 엔드포인트 제거)
- Modify: `query-service/src/index.ts` (stream 엔드포인트 추가)

- [ ] **Step 1: imports 추가**

`query-service/src/index.ts` 상단 import 블록에 추가:

```typescript
import { stream } from "hono/streaming";
import { formatEvent } from "./sse/format.js";
```

- [ ] **Step 2: 기존 status 엔드포인트 제거**

`query-service/src/index.ts:178-201` 의 `app.get("/query/status/:jobId", ...)` 블록을 통째로 삭제.

- [ ] **Step 3: SSE stream 엔드포인트 추가**

삭제된 위치(또는 `app.get("/health", ...)` 위)에 추가:

```typescript
app.get("/query/stream/:jobId", (c) => {
  const jobId = c.req.param("jobId");
  const job = queryJobs.get(jobId);
  if (!job) return c.json({ error: "job not found or expired" }, 404);

  // 헤더는 stream() 호출 전 — body 시작 후엔 무효
  c.header("Content-Type", "text/event-stream");
  c.header("Cache-Control", "no-cache");
  c.header("Connection", "keep-alive");
  c.header("X-Accel-Buffering", "no");

  return stream(c, async (s) => {
    let resolveDone: (() => void) | null = null;
    const donePromise = new Promise<void>((r) => {
      resolveDone = r;
    });

    // 1. 먼저 구독해서 race window 제거
    const unsubscribe = queryJobs.subscribe(jobId, async (updated) => {
      await s.write(formatEvent(updated)).catch(() => {});
      if (updated.status === "completed" || updated.status === "failed") {
        await s.write("event: done\ndata: ok\n\n").catch(() => {});
        unsubscribe();
        resolveDone?.();
      }
    });

    // 2. 그다음 현재 스냅샷 전송 (subscribe 후 update 즉시 와도 유실 없음)
    //    progressIndex 단조 증가라 동일 단계 중복 전송돼도 클라이언트 idempotent
    const current = queryJobs.get(jobId);
    if (current) {
      await s.write(formatEvent(current));
      if (current.status === "completed" || current.status === "failed") {
        await s.write("event: done\ndata: ok\n\n");
        unsubscribe();
        resolveDone?.();
      }
    }

    // 3. heartbeat 10초
    const heartbeat = setInterval(() => {
      s.write(":heartbeat\n\n").catch(() => clearInterval(heartbeat));
    }, 10_000);

    // 4. 클라이언트 abort 시 정리
    c.req.raw.signal.addEventListener("abort", () => {
      unsubscribe();
      clearInterval(heartbeat);
      resolveDone?.();
    });

    await donePromise;
    clearInterval(heartbeat);
  });
});
```

- [ ] **Step 4: 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/query-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 5: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add query-service/src/index.ts && git commit -m "feat(query): SSE /query/stream endpoint, remove /query/status"
```

---

## Task 4: eval runner 폴링 → 수동 SSE 파서

**Files:**
- Modify: `query-service/src/eval/runner.ts:53-116` (callQueryService + interfaces)

- [ ] **Step 1: 사용하지 않는 인터페이스/상수 제거**

`query-service/src/eval/runner.ts:53-65` 블록 삭제:

```typescript
// 삭제 대상
interface JobStatusResponse { ... }
const POLL_INTERVAL_MS = 1000;
const POLL_TIMEOUT_MS = 120_000;
```

대신 SSE timeout만 추가:

```typescript
const SSE_TIMEOUT_MS = 120_000;
```

- [ ] **Step 2: callQueryService 본체를 SSE 구독으로 교체**

`query-service/src/eval/runner.ts:67-116` 의 `callQueryService` 본체를 다음으로 교체:

```typescript
async function callQueryService(
  snapshot: EvalSnapshotRow,
  runId: string
): Promise<QueryResponse> {
  const url = process.env.QUERY_SERVICE_URL ?? `http://localhost:${process.env.PORT ?? 8082}`;
  const token = process.env.INTERNAL_AUTH_TOKEN;
  if (!token) throw new Error("INTERNAL_AUTH_TOKEN required for eval runner");

  // 1. 비동기 시작
  const startRes = await fetch(`${url}/query`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-Internal-Token": token,
      "X-User-ID": snapshot.user_id,
      "X-Document-ID": snapshot.document_id,
      "X-Eval-Run-Id": runId,
    },
    body: JSON.stringify({ question: snapshot.question }),
  });

  if (!startRes.ok) {
    throw new Error(`query-service start ${startRes.status}: ${await startRes.text()}`);
  }
  const { jobId } = (await startRes.json()) as { jobId: string };

  // 2. SSE 구독 (Node native fetch + ReadableStream)
  const ac = new AbortController();
  const timeout = setTimeout(() => ac.abort(), SSE_TIMEOUT_MS);
  try {
    const streamRes = await fetch(`${url}/query/stream/${jobId}`, {
      headers: { "X-Internal-Token": token, Accept: "text/event-stream" },
      signal: ac.signal,
    });
    if (!streamRes.ok || !streamRes.body) {
      throw new Error(`stream ${streamRes.status}: ${await streamRes.text().catch(() => "")}`);
    }

    const reader = streamRes.body.getReader();
    const decoder = new TextDecoder();
    let buffer = "";

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      buffer += decoder.decode(value, { stream: true });

      // \n\n 경계로 SSE 이벤트 분리
      let idx: number;
      while ((idx = buffer.indexOf("\n\n")) !== -1) {
        const raw = buffer.slice(0, idx);
        buffer = buffer.slice(idx + 2);

        const dataLine = raw.split("\n").find((l) => l.startsWith("data: "));
        if (!dataLine) continue;
        const payload = dataLine.slice(6);
        if (payload === "ok" || payload === "error") continue; // event: done 마커

        const data = JSON.parse(payload);
        if (data.status === "completed" && data.result) {
          return {
            answer: data.result.answer,
            citations: data.result.citations,
            retrieved_clauses: data.result.retrieved_clauses,
          };
        }
        if (data.status === "failed") {
          throw new Error(`query failed: ${data.error ?? "unknown"}`);
        }
      }
    }
    throw new Error("stream ended without completion");
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **Step 3: 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/query-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add query-service/src/eval/runner.ts && git commit -m "feat(eval): callQueryService consume SSE instead of polling"
```

---

## Task 5: ui-service SSE 프록시 라우트 추가 + 기존 status 라우트 제거

**Files:**
- Create: `ui-service/app/api/query/stream/[jobId]/route.ts`
- Delete: `ui-service/app/api/query/status/[jobId]/route.ts`

- [ ] **Step 1: stream 라우트 신규 작성**

`ui-service/app/api/query/stream/[jobId]/route.ts` 작성:

```typescript
import { NextRequest, NextResponse } from "next/server";
import { createClient } from "../../../../lib/supabase/server";
import { getJobDocumentId, markAssistantSaved } from "../../../../lib/queryJobStore";

export async function GET(
  req: NextRequest,
  { params }: { params: Promise<{ jobId: string }> }
) {
  const { jobId } = await params;

  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 });
  }

  const queryUrl = process.env.QUERY_API_URL;
  const upstream = await fetch(`${queryUrl}/query/stream/${jobId}`, {
    headers: {
      "X-Internal-Token": process.env.INTERNAL_AUTH_TOKEN ?? "",
      Accept: "text/event-stream",
    },
    signal: req.signal,
  });

  if (upstream.status === 404) {
    return NextResponse.json({ error: "job not found or expired" }, { status: 404 });
  }
  if (!upstream.ok || !upstream.body) {
    return NextResponse.json(
      { error: "upstream error" },
      { status: upstream.status }
    );
  }

  const documentId = getJobDocumentId(jobId);
  const decoder = new TextDecoder();
  let buffer = "";

  const transform = new TransformStream<Uint8Array, Uint8Array>({
    async transform(chunk, controller) {
      // 1. 패스스루 — 브라우저 진행 표시는 즉시
      controller.enqueue(chunk);

      // 2. 버퍼에 쌓고 \n\n 경계로 SSE 이벤트 단위 파싱
      buffer += decoder.decode(chunk, { stream: true });
      let idx: number;
      while ((idx = buffer.indexOf("\n\n")) !== -1) {
        const raw = buffer.slice(0, idx);
        buffer = buffer.slice(idx + 2);

        const dataLine = raw.split("\n").find((l) => l.startsWith("data: "));
        if (!dataLine) continue;
        const payload = dataLine.slice(6);
        if (payload === "ok" || payload === "error") continue;

        try {
          const data = JSON.parse(payload);
          if (
            data.status === "completed" &&
            data.result &&
            documentId &&
            markAssistantSaved(jobId)
          ) {
            await supabase.from("messages").insert({
              document_id: documentId,
              user_id: user.id,
              role: "assistant",
              content: data.result.answer,
              citations: data.result.citations ?? [],
            });
          }
        } catch (err) {
          console.error(`[stream-route] ${jobId}: parse/save failed:`, err);
        }
      }
    },
  });

  return new Response(upstream.body.pipeThrough(transform), {
    headers: {
      "Content-Type": "text/event-stream",
      "Cache-Control": "no-cache, no-transform",
      Connection: "keep-alive",
      "X-Accel-Buffering": "no",
    },
  });
}
```

- [ ] **Step 2: 기존 status 라우트 파일 삭제**

```bash
rm "/Users/yuhojin/Desktop/QA Agent/insurance-qa-agent/ui-service/app/api/query/status/[jobId]/route.ts"
```

빈 디렉토리도 제거:

```bash
rmdir "/Users/yuhojin/Desktop/QA Agent/insurance-qa-agent/ui-service/app/api/query/status/[jobId]" 2>/dev/null
rmdir "/Users/yuhojin/Desktop/QA Agent/insurance-qa-agent/ui-service/app/api/query/status" 2>/dev/null
```

- [ ] **Step 3: ui-service 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/ui-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add -A ui-service/app/api/query && git commit -m "feat(ui): SSE proxy route, remove status polling route"
```

---

## Task 6: ChatPanel EventSource 전환

**Files:**
- Modify: `ui-service/app/components/ChatPanel.tsx:14` (POLL_INTERVAL_MS 제거)
- Modify: `ui-service/app/components/ChatPanel.tsx:48-121` (폴링 useEffect → EventSource)

- [ ] **Step 1: 사용 안 하는 상수 제거**

`ui-service/app/components/ChatPanel.tsx:14` 의 다음 줄 삭제:

```typescript
const POLL_INTERVAL_MS = 1000;
```

- [ ] **Step 2: 폴링 useEffect를 EventSource로 교체**

`ui-service/app/components/ChatPanel.tsx:48-121` 의 `// Polling` useEffect 블록 전체를 다음으로 교체:

```typescript
  // SSE 구독
  useEffect(() => {
    if (!activeJobId) return;

    const es = new EventSource(`/api/query/stream/${activeJobId}`);
    const cleanCloseRef = { current: false };
    const timeout = setTimeout(() => {
      cleanCloseRef.current = true;
      es.close();
      setMessages((prev) => [
        ...prev,
        {
          role: "assistant",
          content: "응답이 지연되고 있어요. 잠시 후 다시 시도해주세요.",
          timestamp: new Date(),
        },
      ]);
      setActiveJobId(null);
    }, 60_000);

    es.onmessage = (event) => {
      const data = JSON.parse(event.data);

      if (data.status === "completed" && data.result) {
        const citations = data.result.citations ?? [];
        cleanCloseRef.current = true;
        clearTimeout(timeout);
        es.close();
        setMessages((prev) => [
          ...prev,
          {
            role: "assistant",
            content: data.result.answer ?? "답변을 받을 수 없습니다.",
            citations,
            timestamp: new Date(),
          },
        ]);
        setCitations(citations);
        setActiveJobId(null);
        return;
      }

      if (data.status === "failed") {
        cleanCloseRef.current = true;
        clearTimeout(timeout);
        es.close();
        setMessages((prev) => [
          ...prev,
          {
            role: "assistant",
            content: "오류가 발생했습니다. 잠시 후 다시 시도해주세요.",
            timestamp: new Date(),
          },
        ]);
        setActiveJobId(null);
        return;
      }

      setProgress({
        stepLabel: data.stepLabel ?? "처리 중",
        progressIndex: data.progressIndex ?? 0,
        totalSteps: data.totalSteps ?? null,
      });
    };

    es.addEventListener("done", () => {
      cleanCloseRef.current = true;
      clearTimeout(timeout);
      es.close();
    });

    es.onerror = () => {
      // 클린 close 후에도 onerror가 fire될 수 있음 → ref로 구분
      if (cleanCloseRef.current) return;
      cleanCloseRef.current = true;
      clearTimeout(timeout);
      es.close();
      setMessages((prev) => [
        ...prev,
        {
          role: "assistant",
          content: "연결이 끊어졌습니다. 다시 시도해주세요.",
          timestamp: new Date(),
        },
      ]);
      setActiveJobId(null);
    };

    return () => {
      cleanCloseRef.current = true;
      clearTimeout(timeout);
      es.close();
    };
  }, [activeJobId, setMessages, setCitations]);
```

- [ ] **Step 3: ui-service 타입 체크**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent/ui-service && npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add ui-service/app/components/ChatPanel.tsx && git commit -m "feat(ui): ChatPanel EventSource subscription, drop polling"
```

---

## Task 7: 빌드 + 배포 + e2e 검증

**Files:** N/A (런타임 검증)

- [ ] **Step 1: 전체 배포 (Docker 빌드 + minikube apply + 헬스체크)**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && bash scripts/deploy.sh query-service ui-service
```

Expected: 모든 헬스체크 통과, port-forward `localhost:3000` 활성

- [ ] **Step 2: 브라우저 e2e — 단일 SSE 연결 확인**

1. http://localhost:3000/dashboard 접속, 로그인
2. DevTools Network 탭 열기
3. 약관 선택 후 질문 입력 ("면책기간이 언제 시작되나요?")
4. Network 탭에서 `/api/query/stream/<jobId>` 요청 **1건**만 확인
5. Type 컬럼이 `eventsource` 또는 `text/event-stream`
6. EventStream 탭에서 단계별 `data: {...}` 이벤트 push 확인
7. 완료 시 `event: done` 수신 + 연결 자동 close

Expected: HTTP 요청 1회로 모든 단계 표시 + 완료, 폴링 없음

- [ ] **Step 3: 중복 가드 동작 확인**

1. 질의 직후 같은 약관에 또 다른 질의 시도
2. 토스트 "이전 질의가 아직 처리 중입니다" 표시
3. 기존 jobId로 SSE 자동 복귀 (현재 진행 단계가 즉시 표시됨)

- [ ] **Step 4: eval cron 동작 확인 (선택)**

```bash
kubectl logs -n insurance-qa deploy/query-service --tail=200 | grep eval-runner
```

Expected: 마지막 cron 시각 기록 또는 다음 fire 대기 메시지

수동 트리거를 위한 별도 엔드포인트는 없음 — 다음 Sunday 03:00 UTC 또는 재배포 시 첫 fire 확인.

- [ ] **Step 5: 404 회귀 확인 — 기존 status 엔드포인트 제거 검증**

```bash
curl -i -H "X-Internal-Token: $(kubectl get secret query-service-secrets -n insurance-qa -o jsonpath='{.data.INTERNAL_AUTH_TOKEN}' | base64 -d)" "http://$(minikube ip):$(kubectl get svc query-service -n insurance-qa -o jsonpath='{.spec.ports[0].nodePort}')/query/status/anything"
```

Expected: `HTTP/1.1 404 Not Found` (라우트 자체가 없음 — Hono 기본 404)

NodePort 없을 시 port-forward로 대체:

```bash
kubectl port-forward -n insurance-qa svc/query-service 8082:8082 &
curl -i -H "X-Internal-Token: <token>" http://localhost:8082/query/status/anything
```

---

## Task 8: 문서 업데이트

**Files:**
- Modify: `docs/STATUS.md`
- Modify: `docs/ROADMAP.md`
- Modify: `docs/ARCHITECTURE.md`

- [ ] **Step 1: STATUS.md 업데이트**

상단 "마지막 업데이트" → `2026-04-18`. "최근 변경 이력" 표 맨 위에 한 줄 추가:

```
| 2026-04-18 | 쿼리 진행 상태 SSE 전환 (폴링 제거, EventSource + EventEmitter pub/sub, eval runner SSE 구독) | `2026-04-18-query-sse.md` |
```

Phase 4 Tier 1 항목 중 "쿼리 진행 상태 UX + 중복 요청 방지" 항목 옆에 `(SSE 전환 완료)` 추가.

- [ ] **Step 2: ROADMAP.md 업데이트**

`### 0. 쿼리 진행 상태 UX + 중복 요청 방지` 섹션의 "최종 구조" 첫 줄에 추가:

```
- 진행 상태 전송: SSE (`GET /query/stream/:jobId`), 쿼리 1건당 HTTP 연결 1회
```

상단 "마지막 업데이트" → `2026-04-18`.

- [ ] **Step 3: ARCHITECTURE.md 업데이트**

"주요 설계 결정" 섹션에 다음 항목 추가 (적절한 위치 — 다른 결정 항목과 동일 포맷):

```markdown
### 쿼리 진행 상태 전송 — SSE

**선택:** Server-Sent Events (단방향, EventSource 표준)

**Why:** 폴링은 30초 쿼리 = 30회 HTTP 왕복으로 Railway 대역폭/CPU 낭비.
SSE는 단방향 push에 표준 부합 + 1회 연결로 충분, WebSocket은 양방향이라 오버스펙.

**구성:**
- query-service: `JobRegistry.subscribe()` (EventEmitter), Hono `stream()` 핸들러
- ui-service: Next.js streaming Response + TransformStream으로 프록시 + assistant 저장
- ChatPanel: 브라우저 표준 `EventSource`
- eval runner: Node native fetch + ReadableStream 수동 SSE 파서 (Node에 EventSource 없음)

**제약:** query-service replica=1 전제 (multi-replica 시 pub/sub broker 필요).
ui-service replica=1 전제 (`markAssistantSaved` in-memory dedup).

**상세 스펙:** `docs/superpowers/specs/2026-04-17-query-sse.md`
```

- [ ] **Step 4: Commit**

```bash
cd /Users/yuhojin/Desktop/QA\ Agent/insurance-qa-agent && git add docs/STATUS.md docs/ROADMAP.md docs/ARCHITECTURE.md && git commit -m "docs: SSE 전환 반영 (STATUS/ROADMAP/ARCHITECTURE)"
```

---

## 검증 체크리스트 (스펙 §검증 기준 매핑)

| 스펙 기준 | 검증 위치 |
|---|---|
| 1. HTTP 연결 1회 | Task 7 Step 2 |
| 2. DevTools Network에 stream 1개 | Task 7 Step 2 |
| 3. 단계 라벨 즉시 push (폴링 1초 지연 없음) | Task 7 Step 2 (EventStream 시점 관찰) |
| 4. 완료 시 EventSource 자동 close | Task 7 Step 2 (Network 상태 → "done") |
| 5. 새로고침/닫기 시 unsubscribe + heartbeat clear | Task 6 Step 2 cleanup return |
| 6. 30초 idle도 connection 유지 (heartbeat) | Task 3 Step 3 (10초 간격) |
| 7. 다중 탭 동일 jobId 구독 | Task 1 Step 3 (EventEmitter, 검증은 옵션) |
| 8. status 엔드포인트 제거 (404) | Task 7 Step 5 |
| 9. eval runner SSE 동작 | Task 7 Step 4 (다음 cron) |
| 10. 409 in-flight → SSE 복귀 | Task 7 Step 3 |

---

## 자체 검토 결과

- [x] **Spec coverage:** 스펙 §변경 범위의 모든 파일이 Task에 1:1 매핑됨 (query-jobs / index / runner / stream route / status route 삭제 / ChatPanel)
- [x] **Placeholder scan:** "TBD/TODO/handle edge cases" 없음. 모든 step이 실제 코드 또는 명령
- [x] **Type consistency:** `formatEvent` 시그니처(QueryJob → string), `subscribe` 반환(unsubscribe fn), `markAssistantSaved` 호출 위치 모두 일관
- [x] **검증 매핑:** 스펙 §검증 기준 10개 모두 Task에 위치 매핑
- [x] **TTL listener 누수:** Task 1 Step 4에서 cleanup 시 `removeAllListeners` 추가
- [x] **Hono 헤더 시점:** Task 3 Step 3에서 `stream()` 호출 전 `c.header()` 호출
- [x] **subscribe-snapshot race:** Task 3 Step 3에서 subscribe 먼저, snapshot 나중
- [x] **chunk 경계 파싱:** Task 4/5에서 buffer + `\n\n` splitter
- [x] **EventSource 클린 close 오탐:** Task 6에서 `cleanCloseRef` + 60s timeout

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-18-query-sse.md`.

CLAUDE.md MANDATORY 규칙에 따라 **Subagent-Driven Development** 방식으로 실행 — Task마다 새 서브에이전트를 디스패치하고 스펙 리뷰 → 코드 퀄리티 리뷰 2단계 검토를 거친다.
